# Large data files — too big for GitHub (14GB total)
# Only exclude the ROOT data folder (14GB raw data)
# air_quality_fyp/data/ is a small sample dataset and IS committed
/data/

# Secrets — never commit these
**/secrets.toml
.env

# Claude Code local config
.claude/

# Python cache
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Environment
.env
venv/
.venv/
